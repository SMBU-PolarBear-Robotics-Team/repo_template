# demo_component

This package provides a minimal composable node that:

- publishes `demo_interfaces/msg/DemoState`
- serves `demo_interfaces/srv/SetMode`
- runs `demo_interfaces/action/CountSteps` as an action server

Launch it with `demo_component_launch.py`.
