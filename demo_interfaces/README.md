# demo_interfaces

This package defines one message, one service, and one action:

- `DemoState.msg`
- `SetMode.srv`
- `CountSteps.action`

They are generated with `rosidl_auto_generate_interfaces()`.
